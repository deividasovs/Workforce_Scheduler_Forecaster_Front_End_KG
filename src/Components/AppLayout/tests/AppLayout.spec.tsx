import { AppLayout } from "../AppLayout";

describe("AppLayout", () => {
    it("should render successfully", () => {
        expect(() => {
            //    <AppLayout />;
        }).not.toThrow();
    });
})
